name: fluttergoogleplaceswebexample
description: An example for FlutterGooglePlacesWeb
publish-to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <3.6.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_google_places_web: 
    path: ../

  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
